# BTC Scenario Advisor · UX audit · 2026-08-19

## P0

- Enforce an authenticated network gate before model/market requests begin. **Implemented and CI validated in merge 0671d06adc563a51c14d3bd91f73560c147b22fd.**
- Keep Premium capabilities sourced from Product Control/Firestore entitlements, never local UI flags.
- Require backend authorization for premium/private API endpoints before commercial launch; the browser gate is defense-in-depth, not backend security.
- Distinguish API offline, no eligible signal, and calibration blocked as different product states.

## P1

- Show Trial 90D as verified progress, including completed days, last verified evidence and next scheduled checkpoint.
- Surface the top Auto-Paper blocking reason with a human-readable explanation and expandable technical detail.
- Replace stale infrastructure revision labels with API contract/runtime metadata returned by the backend.
- Avoid execution language such as `LIVE_EXECUTION_OVERLAY` while trading execution remains forbidden.
- Rename the user preference `minProbability` to an alert/notification threshold and state explicitly that it never changes Quant eligibility.

## P2

- Consolidate Cuenta into Identity, Plan, Alert channels and Security sections.
- Use progressive disclosure for Quant metrics. The first screen should answer: what is happening, can the model act, and why/why not.
- Keep mobile primary actions reachable without horizontal scrolling and preserve 320px support.
- Add PWA/push only as informational delivery while the project remains SHADOW.

## Product principle

Confidence never equals execution authority. `SHADOW_MODE=true`, `SPOT_ONLY`, no SELL, no shorts and no real orders remain explicit until a separately governed execution phase exists.
