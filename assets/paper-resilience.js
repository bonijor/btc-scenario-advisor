const D=typeof document==='undefined'?null:document,$=id=>D?.getElementById(id),set=(id,v)=>{const e=$(id);if(e)e.textContent=v};
export function normalizedPaperPayload(payload){const root=payload&&typeof payload==='object'?payload:{},base=root.paper&&typeof root.paper==='object'?root.paper:root,metrics=base.metrics&&typeof base.metrics==='object'?base.metrics:{},trades=Array.isArray(base.trades)?base.trades:Array.isArray(root.trades)?root.trades:[];return{simulatedTrades:root.simulatedTrades??base.verified??metrics.verifiedTrades??0,winRatePct:root.winRatePct??metrics.winRatePct??null,netPnlPct:root.netPnlPct??metrics.netReturnPct??null,drawdownPct:root.drawdownPct??metrics.maxDrawdownPct??null,trades,note:root.note||(root.status?`Estado Paper: ${root.status}. Sólo se publican operaciones con entrada, salida y evidencia verificadas.`:''),funnel:root.funnel||null}}
function ensureFunnelUi(){if($('paperFunnelPanel')||!D)return;const table=D.querySelector('#paper .tableWrap');if(!table)return;table.insertAdjacentHTML('beforebegin','<article class="panel content" id="paperFunnelPanel" style="margin-top:10px"><div class="head"><div><div class="ey">Embudo de elegibilidad</div><h3>De observación a señal Paper elegible</h3></div><span class="chip" id="funnelStatus">sin datos</span></div><p class="tiny">Cada etapa muestra cuántas observaciones siguen vivas después de aplicar los filtros del motor. Cero elegibles no significa cero datos.</p><div class="analyticsGrid"><div class="analyticsCard"><span>Observadas</span><strong id="funnelObserved">--</strong></div><div class="analyticsCard"><span>Horizonte oficial</span><strong id="funnelOfficial">--</strong></div><div class="analyticsCard"><span>Sesgo bullish</span><strong id="funnelBullish">--</strong></div><div class="analyticsCard"><span>Alta confianza</span><strong id="funnelConfidence">--</strong></div><div class="analyticsCard"><span>Elegibles</span><strong id="funnelEligible">--</strong></div></div><div class="bannerNote" id="funnelReasons">Esperando evidencia del funnel.</div></article>')}
export function renderFunnel(funnel){ensureFunnelUi();if(!$('paperFunnelPanel'))return;const data=funnel&&typeof funnel==='object'?funnel:null;if(!data){for(const id of['funnelObserved','funnelOfficial','funnelBullish','funnelConfidence','funnelEligible'])set(id,'--');set('funnelStatus','sin datos');set('funnelReasons','Esperando evidencia del funnel.');return}const counts=data.counts||{},value=key=>Number.isFinite(+counts[key])?String(+counts[key]):'--';for(const [id,k] of [['funnelObserved','observed'],['funnelOfficial','officialHorizon'],['funnelBullish','bullishBias'],['funnelConfidence','highConfidence'],['funnelEligible','eligible']])set(id,value(k));set('funnelStatus',`protocolo ${data.protocol||'read-only'}`);const rejected=data.rejectedByReason&&typeof data.rejectedByReason==='object'?Object.entries(data.rejectedByReason):[],lifecycle=data.lifecycle||{},reason=rejected.length?rejected.map(([key,n])=>`${key}: ${n}`).join(' · '):'Sin rechazos publicados.';set('funnelReasons',`${reason} · abiertas: ${Number(lifecycle.opened||0)} · verificadas: ${Number(lifecycle.verified||0)}`)}